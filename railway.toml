# Railway Project Configuration
# Deploy all services with correct paths and environment variables

[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/actuator/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
